export default async function handler(req, res) {
  const { mode } = req.body;

  export default async function handler(req, res) {
    return res.status(200).json({
      text: "What is overfitting in machine learning?"
    });
  }

  // OpenAI code (never runs)
}
